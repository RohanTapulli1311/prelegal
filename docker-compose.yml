services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./.env:/app/.env:ro
      - db_data:/app/data
    environment:
      - DB_PATH=/app/data/db.sqlite3

  frontend:
    build:
      context: ./frontend
      args:
        NEXT_PUBLIC_API_URL: http://localhost:8000
    ports:
      - "3000:3000"
    depends_on:
      - backend

volumes:
  db_data:
